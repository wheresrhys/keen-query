class Concat {
	constructor (q1, q2, sharedQ) {
		// append sharedQ to both q
		// if is a data printer type don't print yet
		// get the data etc, tabulate and print

		// hmm probably need to tabulate all the data
	}
}

module.exports = Concat;
